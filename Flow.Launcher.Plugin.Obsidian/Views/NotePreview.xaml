<UserControl
    x:Class="Flow.Launcher.Plugin.Obsidian.Views.NotePreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="12,10,12,6">
            <TextBlock
                x:Name="TitleTextBlock"
                FontSize="16"
                FontWeight="SemiBold"
                TextWrapping="Wrap" />
            <TextBlock
                x:Name="PathTextBlock"
                Margin="0,2,0,0"
                FontSize="11"
                TextWrapping="Wrap" />
        </StackPanel>

        <Rectangle
            x:Name="HeaderSeparator"
            Grid.Row="1"
            Height="1"
            HorizontalAlignment="Stretch" />

        <FlowDocumentScrollViewer
            x:Name="DocumentViewer"
            Grid.Row="2"
            Padding="12,8,12,12"
            VerticalScrollBarVisibility="Auto" />
    </Grid>
</UserControl>
